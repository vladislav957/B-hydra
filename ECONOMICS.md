# Экономика B-hydra — раздел для белой книги

Готовый текст для белой книги. Лежит в репозитории рядом с кодом намеренно:
числа здесь — не пересказ, а следствие констант из `b_hydra/blockchain.py`, и
они закреплены тестами (`tests/test_economics.py`). Меняются константы —
падает тест, и текст правится вместе с ними.

Проверить всё, что ниже, одной командой:

```bash
python -m b_hydra.economics
```

---

## Параметры эмиссии

| Параметр | Значение |
|---|---|
| Начальная награда за блок | 50 BHY |
| Интервал халвинга | **310 000 блоков** (≈ 28,6 года) |
| Целевое время блока | ≈ 48,6 мин (2916 с) |
| Максимальная эмиссия | 31 000 000 BHY |
| Делимость | 1e-8 BHY |
| Год генезиса | 2026 |
| Конец эмиссии | ≈ 3000 год, блок 10 540 000 |

Потолок не задан отдельным числом «сверху», а получается сам:

```
310 000 (интервал халвинга) × 50 (награда) × 2  =  31 000 000 BHY
```

Множитель 2 — сумма геометрической прогрессии 1 + ½ + ¼ + … = 2, то есть все
последующие эпохи вместе дают ровно столько же, сколько первая.

## Правило халвинга

Награда зависит **только от высоты блока**:

```
R(h) = R₀ · 2^(−⌊h / H⌋),   R₀ = 50 BHY,   H = 310 000 блоков
```

⚠️ **Халвинг считается по высоте, а не по календарю.** Это принципиально: если
бы награда зависела от даты, узел с неверно идущими часами насчитал бы другую
награду и отверг бы честный блок — то есть сеть разошлась бы из-за настроек
времени на чьём-то компьютере. Дат в расчёте награды нет вовсе.

Годы — это **следствие** целевого времени блока, а не обещание:

```
T_халвинга = H · t_блока = 310 000 × 2916 с ≈ 904 032 000 с ≈ 28,6 года
```

Фактический срок гуляет вокруг этого значения вместе с хешрейтом сети: стало
больше майнеров — блоки идут быстрее цели, и халвинг придёт раньше; меньше —
позже. Сложность подтягивает темп обратно к цели (LWMA, пересчёт на каждом
блоке), но мгновенно этого не происходит.

## Календарь эпох

| Эпоха | Высота | Награда | Примерно с |
|---|---|---|---|
| 0 | 0 | 50 BHY | 2026 |
| 1 | 310 000 | 25 BHY | 2055 |
| 2 | 620 000 | 12,5 BHY | 2083 |
| 3 | 930 000 | 6,25 BHY | 2112 |
| … | … | … | … |
| 34 | 10 540 000 | < 1e-8 BHY | ≈ 3000 |

Эмиссия заканчивается, когда награда становится меньше наименьшей единицы
монеты (1e-8 BHY) — после 34 халвингов. Дальше майнеры живут только на
комиссиях.

Время блока подобрано **из** этой цели, а не наоборот:

```
t_блока = (3000 − 2026) × секунд_в_году / 10 540 000 ≈ 2916 с
```

## Чего в модели нет

* **Премайна и предварительной продажи.** Генезис-блок не создаёт монет
  вообще (эмиссия после него — ровно 0), первый добытый блок даёт такие же
  50 BHY, как и любой другой. Выделенных адресов у создателей нет.
* **Инфляции после конца эмиссии.** Потолок 31 000 000 BHY жёсткий: попытка
  выдать в coinbase больше, чем «награда + комиссии», делает блок невалидным,
  и это проверяет каждый узел.
* **Изменения награды голосованием.** Награда — правило консенсуса; узел со
  своей формулой просто окажется в другой сети.

---

## Исправления к прежней редакции белой книги

Ниже — то, что в предыдущей редакции не совпадало с кодом. Числа проверены
запуском, а не вычитаны глазами.

| Было | Стало | Почему |
|---|---|---|
| «халвинг каждые 4 года» | **раз в 310 000 блоков (≈ 29 лет)** | 4 года — параметр Bitcoin; к нашим 310 000 блоков по 48,6 мин отношения не имеет |
| `R(t) = R₀·(½)^((t−t₀)/T)` — по времени | `R(h) = R₀·2^(−⌊h/H⌋)` — по высоте | по календарю награда зависела бы от часов на машине, и сеть расходилась бы из-за неверного времени |

Ещё три расхождения белой книги с кодом **не** исправлены здесь — это разные
системы, а не уточнения формулировок, и решение по ним за автором:

* **Proof of Contribution** (баллы за пул-реквесты, статьи) — в коде
  **Proof of Work** на SHA-512, выбор главной цепи по суммарной работе;
* **связь D2D по Wi-Fi и Bluetooth** — в коде TCP/IP; UDP-маяк находит узлы
  только внутри одной локальной сети;
* **«кибертронский хеш — SHA-512 с модификациями»** — в коде обычный SHA-512,
  байт-в-байт совпадающий с `hashlib` и любой сторонней реализацией.

Последнее стоит пояснить отдельно: **модифицировать SHA-512 нельзя**. Не
потому, что сложно, а потому, что тогда хеши перестанут сходиться с любым
независимым инструментом, и проверить нашу цепочку снаружи станет невозможно.
Сейчас кто угодно может пересчитать любой наш блок чужим кодом и убедиться,
что мы не жульничаем; это и есть доказуемая честность. Стойкость от
«модификаций» не растёт — она у SHA-512 и так избыточна, — а проверяемость
теряется целиком.
