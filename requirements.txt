# B-hydra — зависимости
#
# Ядро (блокчейн, кошелёк с ECDSA, транзакции, нода, P2P, CLI) работает
# ТОЛЬКО на стандартной библиотеке Python 3.9+ и не требует установки пакетов.
#
# Пакеты ниже нужны лишь для отдельных необязательных модулей:

# Ускоритель ECDSA: нативный secp256k1 (libsecp256k1). Проверка подписи
# ускоряется в ~50× (20 мс → 0.36 мс). Без него кошелёк работает на чистом
# Python — просто медленнее. Включается автоматически после self-test.
coincurve>=18.0

# RSA.py — подпись RSA + SHA-512/PSS (учебный RSA работает и без неё)
cryptography>=42.0

# QR.py — генерация QR-кода для адреса
qrcode[pil]>=7.4

# tools/make_icon.py — генерация иконки приложения (необязательно)
pillow>=10.0

# Для запуска автотестов (необязательно): pip install pytest && pytest
pytest>=8.0

# Для сборки .exe/.app/бинаря десктоп-клиента (необязательно): см. BUILD.md
pyinstaller>=6.0
